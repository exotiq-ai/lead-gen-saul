{
  "name": "saul-provider-phone-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ElevenLabs Custom-LLM webhook for Saul provider-sales phone agent, with GHL-first lead capture and booking.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "wrangler dev --port 8791",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types 'src/**/*.test.ts'",
    "simulate": "node --experimental-strip-types scripts/simulate-call.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/node": "^25.6.2",
    "typescript": "^5.6.0",
    "wrangler": "^3.95.0"
  }
}
